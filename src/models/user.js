class User {
    constructor(id, firstName, lastName, speech) {        
        this.id = id;
        this.firstName = firstName;
        this.lastName = lastName;
        this.speech = speech;
    }
}

export default User;